# CodexHub 0.2.5

Follow-up to the published 0.2.4 opt-in preview. Restart CodexHub after installing so the updated Gateway loads.

- Replay known Codex CLI 0.153.4 Collaboration V2 plain-text handler failures through history instead of rejecting them as `malformed_collaboration_result`. Covers duplicate agent paths, invalid `fork_turns`, empty messages, follow-up targeting root, missing agent paths, collab-manager unavailability on spawn/list/send/followup, and paired integer `wait_agent` timeout range errors. New calls stay strictly validated; V1 stays isolated.
- Keep structured tool-result images during compact: lift them into adjacent messages when the route can carry them, use placeholders only for trusted compact on text-only routes, and leave official passthrough unchanged.
- Replace inbound client User-Agent on third-party compatibility routes so isolated clients are not blocked by browser-integrity checks; official passthrough still keeps the caller UA.
- Map official Chat Completions onto native Codex tool surfaces: portable reasoning, first-class `prompt_cache_key`, Collaboration V2 namespace expansion, and reversible hosted/custom/`tool_search` aliases.

After installing, quit completely and reopen CodexHub. Do not delete conversation history. If problems occur, return to 0.2.4; do not roll back conversation data merely to roll back the application.
