# CodexHub 0.2.8

- Consolidate Gateway request, attempt, stream, and tool-history ownership while preserving protocol behavior.
- Fix compact continuation with parallel tool-call history.
- Preserve client image-tool names when xAI native search uses a conflicting name, including streamed responses and subsequent history.
- Enable client-side tool discovery on compatible third-party routes. Preserve discovered schemas, clear resolved defer-loading markers, and respect explicit protocol/capability restrictions.
- Remove unsupported Official-only request fields on the affected Console Go Responses route.

Linux and Windows are built from the same release commit. Linux requires host Python 3.13+; Windows includes its embedded runtime.

After installing, quit CodexHub completely (including the tray process) and reopen it so the new Gateway loads. Refresh the model catalog and let clients reload it to use tool discovery. Do not delete conversation history. Application rollback does not require conversation-data rollback.
