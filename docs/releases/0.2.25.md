# CodexHub 0.2.25

- Claude Code can retain its native subscription login while using configured Gateway models in the same conversation.
- Fix model switching across native Anthropic Messages, Codex Responses, and third-party Responses/Chat/Messages routes. Conversation text and tool results retain their call associations.
- Fix custom-model validation when an upstream streams a response to a non-streaming request.
- Preserve explicit native model selection and separate default, family-alias, and subagent settings.
- Keep Claude's subscription classifier context on the native route; external routes use a named compatibility adaptation without fabricating permission decisions.
- Report unsupported reasoning-history conversions explicitly, while allowing portable reasoning responses to complete.
- Record connected native subscription usage in the existing Usage Statistics flow.
- Fix Windows Claude discovery with UTF-8 CLI output under a non-UTF-8 system locale, and batch-shim paths containing spaces or ampersands (`&`).

Live Linux source qualification used Claude Code 2.1.283 with Opus 5.5, Codex Sol/Luna, and DeepSeek Flash across all three upstream protocols. Eighteen tool/recall turns and 48 requests passed, including low/max effort, manual compaction and explicit same-session resume. This evidence covers the named models and paths, not every provider-specific capability.

Linux requires Python 3.13+. After installing, fully quit CodexHub (including its tray process) and reopen it to load the new Gateway. Existing conversation files and provider credentials are preserved.

Claude Code 可保留原生订阅登录，在同一会话中切换原生模型、Codex 订阅模型和已配置的第三方模型。修复跨 Messages、Responses、Chat Completions 路由的历史与工具关联，保留完整原生模型 ID 和显式续接，并记录原生订阅用量。Windows 同时修复了非 UTF-8 系统区域设置下的 CLI 输出解码，以及路径含空格或 `&` 时的 Claude 批处理启动。

隔离 Linux 实测使用 Claude Code 2.1.283，完成 18 轮工具/记忆校验、48 个成功请求，覆盖 Opus 5.5、Codex Sol/Luna Responses，以及 DeepSeek Flash 的 Responses/Chat/Messages，包含手动压缩、显式续接和返回原生模型。

The release qualification contract uses Official `gpt-6-luna` and official DeepSeek `deepseek-flash`: all four CLI clients must pass separately on Linux and Windows, with an independent inbound Chat gate. This is an explicitly approved identity update, with no gate waiver. Earlier unavailable-model and OpenCode Go quota/rate-limit results retain their original model and SHA identities. Issue #564 and the published release evidence record the exact packaged candidate, CLI versions and passed, unsupported or unverified compatibility rows, including subscription usage, subagents, too-long recovery, cancellation and cache reuse. Historical source results alone do not qualify a new package; publication requires the current candidate's required gates to pass.

发布验证契约使用 Official `gpt-6-luna` 与 DeepSeek 官方 API `deepseek-flash`：四个 CLI 客户端须分别在 Linux 和 Windows 通过，入站 Chat 单独验证。这是用户明确批准的模型更新，未豁免门禁；此前模型不可用和 OpenCode Go 额度/限流结果保留原模型与 SHA 身份。#564 与正式发布证据记录安装包候选、CLI 版本，以及原生订阅用量、子代理、超长恢复、取消、缓存复用等兼容项的通过、不支持或未验证状态。历史源码结果不能代替新安装包验证，发布须等待当前候选的必需门禁通过。安装后请完整退出 CodexHub（包括托盘进程）并重新打开，新版 Gateway 才会生效；现有会话和凭据保留。
